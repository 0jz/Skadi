<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        QUERY_ALL_PACKAGES: required for the package scanner to see third-party apps
        on Android 11+. Demo build only. A production release must either justify
        this to Google Play or use targeted <queries> entries instead.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Biometric anchor: verifies device owner before entering secret layer -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
        BLE tracker detection: scans for nearby AirTags, Tile, SmartTag.
        Android 12+: BLUETOOTH_SCAN with neverForLocation (no location perm needed).
        Pre-12: BLUETOOTH + BLUETOOTH_ADMIN (scan silently skipped if unavailable).
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        andro